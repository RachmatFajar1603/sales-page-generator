{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --force && vendor/bin/heroku-php-apache2 public/",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
